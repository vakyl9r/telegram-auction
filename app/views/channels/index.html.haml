%h1.text-center Channels
.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr.info
        %th ID
        %th Name
        %th Link
        %th Rules
        %th.text-center.thin-column{colspan: 2}= 'Actions'
    %tbody
      - @channels.each do |channel|
        %tr
          %td= channel.id
          %td= channel.name
          %td= channel.link
          %td= channel.rules
          %td.text-center
            = link_to 'Edit', edit_channel_path(channel), class: 'btn btn-default'
          %td.text-center
            = link_to 'Destroy', channel, method: :delete, class: 'btn btn-danger',
              data: { confirm: 'Are you sure?' }
